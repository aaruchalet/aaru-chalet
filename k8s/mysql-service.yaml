apiVersion: v1
kind: Service

metadata:
  name: aaru-chalet-mysql-service

spec:
  selector:
    app: aaru-chalet-mysql

  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306

  type: ClusterIP
